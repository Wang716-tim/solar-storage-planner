# 测试说明

## 命令

```bash
npm test
```

等效命令：

```bash
node --test
```

## 当前覆盖

- 单元：有效场景的容量、发电、储能和收益结果为有限正值。
- 边界：小屋顶面积限制组件数；零初始投入的回收期为零。
- 错误处理：负用电量、缺失或非数字的发电量参数会得到字段级错误；无效百分号编码得到 HTTP 400。
- 集成：HTTP 服务返回中文页面和正确内容类型。
- 安全回归：访问项目根目录之外的文件得到 403。

## 已执行结果

2026-09-25：`npm test`，10 项通过，0 项失败。

自动测试不代替现场测量或工程审查。窄屏页面与越界输入提示已做浏览器手动检查；桌面宽屏检查尚未完成。
