# 协作约定

- 项目文档是长期状态的唯一依据；开始开发前先看 `README.md` 与 `docs/` 中的上下文、产品、架构、路线图、任务和交接记录。
- 修改前查看 `git status`、`git diff`、`git log`；不要覆盖用户已有改动。
- 每轮聚焦一个可验证的产品目标。计算逻辑放在 `src/engineering.js`，界面交互放在 `src/app.js`，页面结构放在 `index.html`。
- 先为工程计算写测试，再实现。边界、无效输入和零值都要覆盖。
- 光伏与储能估算由确定性公式负责。模型或自然语言层不能替代公式，也不能改变计算结果。
- 所有用户可见文字使用简体中文。页面只描述产品用途和计算假设，不放职业、面试、求职或作品集相关内容。
- 不提交密钥、真实用户用电数据或未核实的工程性能承诺。
- 每轮收尾更新 `TASKS.md`、`CHANGELOG.md` 和 `SESSION_HANDOFF.md`；同步检查 README 与路线图。
